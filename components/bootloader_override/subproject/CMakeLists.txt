cmake_minimum_required(VERSION 3.16)

# This is the bootloader project CMakeLists.txt
# It defines the custom bootloader that will be built instead of the default one

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(bootloader)
